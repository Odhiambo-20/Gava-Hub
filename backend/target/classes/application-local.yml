spring:
  config:
    activate:
      on-profile: local
  jpa:
    show-sql: false

management:
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.gavahub: DEBUG
    org.springframework.web: INFO
