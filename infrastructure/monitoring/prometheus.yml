global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    application: gava-hub

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["prometheus:9090"]

  - job_name: gava-hub-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["backend:8080"]
